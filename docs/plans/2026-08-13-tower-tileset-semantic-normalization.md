# 高塔环境贴图语义统一实施计划

1. 新增跨主题像素契约测试，覆盖固定物件、门、水岸透明度和图集尺寸。
2. 提取共享的高塔图集语义工具，按 alpha 掩模生成水岸并复制固定物件。
3. 接入五个可再生图集脚本，并为哥特城堡增加同一套后处理。
4. 修正门本体与顶部槽位，使竖向和横向墙体中的组合方向一致。
5. 在 `TerrainFeaturesTilemap` 中屏蔽高塔分支的区域植被覆盖，并添加 Java 回归测试。
6. 重新生成六套资产，制作放大审查图，检查接缝、轮廓和语义。
7. 运行六套 Python 测试及相关 Gradle 测试。
